--- GROUP BY
-- mỗi KH trả bao nhiêu tiền

SELECT CUSTOMER_ID,
SUM(AMOUNT) AS TOTAL_AMOUNT,
FROM PAYMENT
GROUP BY CUSTOMER_ID;

--- HAVING

SELECT CUSTOMER_ID,
SUM(AMOUNT) AS TOTAL_AMOUNT,
FROM PAYMENT
GROUP BY CUSTOMER_ID
HAVING SUM(AMOUNT) > 100;
